---
title: "Проверка гипотез в R"
author: "Lulin Dmitry"
date: "2024-11-28"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```
### 1.1. Импорт данных для работы
Подключаем необходимую для считывания данных библиотеку.
```{r}
library('rio')
```
Импортируем наш файл и выбираем соответствующий варианту столбец.
```{r}
dataframe <- import('R_data.xlsx') 
data <- dataframe['v17']
```
Просмотрим первые и последние 5 наблюдений по выборке.
```{r}
head(data)
tail(data)
``` 
В конце выборки имеет одно лишнее значение, уберем его с помощью среза.
```{r}
data <- data[1:100,]
data <- as.data.frame(as.numeric(data))
```
Переименуем столбец данных в соответствии с исследуемой величиной и проверим еще раз наблюдения.
```{r}
names(data) <- 'Объем основных фондов'
head(data)
tail(data)
```
Теперь можем приступать к второму разделу заданий лабораторной работы.

# Раздел 2. Проверка гипотез.

### 2.1. Тесты на однородность дисперсий независимых выборок

Предположим, что мы имеем две выборки X и Y, извлеченные из двух нормально распределенных независимых генеральных совокупностей X∈N(μ1;σ1);Y∈N(μ2;σ2). Выборки взяты из данных типового расчета и поделены на 2 части: создадим две переменные X и Y, приняв за X первые 50 наблюдений нашей выборки, за Y – последние 50:
```{r}
x <- data[1:50,]
y <- data[51:100,]
```
За уровень значимости возьмем 0,05.
```{r}
alpha <- 0.05 
```
Для проведения данного теста при оговоренных допущениях используется 
F-статистика Фишера. Поскольку для теста Фишера используется 
правосторонняя критическая область, это необходимо указать в качестве 
аргумента функции как `alternative = "greater"`: 
```{r}
var.test(x,y, alternative = "greater")
```
Результаты теста включают: $F_{наблюдаемое}$ = 0.98261, $p−value$ = 0.5244. То есть все, что необходимо для проверки нулевой гипотезы. Так как $p-value$ = 0.5244 больше, чем $\alpha$=0,05, то $H_{0}$ не отвергается. Значит, можно считать на уровне значимости $\alpha$=0,05, что гипотеза о равенстве дисперсий генеральных совокупностей не отвергается на уровне значимости $\alpha$=0,05.Тест, кроме того, выдает выборочное отношение дисперсий двух выборок (здесь 0.982606) и его интервальную оценку (она включает 1 и поэтому гипотеза $H_{0}$ не отвергается).

В нашем случае при использовании правосторонней критической области данного критерия нужна вероятность правого хвоста распределения, т.е. $P[X ≥x]$, поэтому задаем это в функции (по умолчанию она выдает квантиль, левый хвост, т.е. вероятность $P[X <x]$).
```{r}
qf(alpha,49,49,lower.tail = FALSE) 
```
Результат показал, что $F_{наблюдаемое}$=0.98261 < $F_{критическое}$=1.607289, следовательно, на уровне значимости 0,05 гипотеза о равенстве дисперсий генеральных совокупностей не отвергается.

### 2.2. Тесты на равенство средних  
#### 2.2.1.  Сравнение среднего совокупности с заданным значением параметрический тест (t-тест Стьюдента)

Провести этот тест в R можно с помощью стандартной встроенной функции `t.test()`. Данная функция имеет аргумент $μ$, который отвечает за параметр проверяемого среднего значения гипотезы $Ho:μ=μ_{0}$. Найдем среднее введенной нами ранее выборки X. Предположим, что выборка X извлечена из нормально распределенной генеральной совокупности $X∈N(μ_x;σ_x)$. Проверим состав нашей переменной с помощью удобной функции проверки целостности данных `glimpse` из пакета `dplyr`:
```{r}
library(dplyr)
glimpse(x)
```
```{r}
mean(x)
```
Получаем результат, показывающий нам, что переменная числовая, состоящая из 50 значений, ее среднее по выборке равно 103.198. Допустим, по нормативу среднее значение Х должно составлять 104. Таким образом, нам по имеющейся в нашем распоряжении выборке необходимо проверить нулевую гипотезу: $H_0:μ=μ_0=104$.

Проведем тест на проверку равенства средней нормативу:
```{r}
t.test(x,mu = 104)
```
Видим, что $p-value$=0.003322 меньше, чем $α$=0,05. Значит, гипотеза $H_0: 𝜇=104$ отвергается на уровне значимости 0,05 (средняя не соответствует нормативу). 
Наблюдаемое значение статистики критерия: `t = -3.0871`. 
Доверительный интервал для генеральной средней по умолчанию строится в рамках теста с надежностью 0,95, в опциях эту вероятность можно изменить. 

$P(102.6759<μ<103.7201)$=0.95

Видим, что он не содержит значение норматива 104. Теперь найдем $t_{крит}$.
```{r}
qt(alpha/2,49)
```
$|t_{набл}|$=3.0871>$t_{крит}$.=2.009575. Значит гипотеза $H_0:μ=μ_0=104$ отвергается на уровне значимости $α$=0.05 (средняя не соответствует нормативу). 

#### 2.2.2. Сравнение среднего выборки с заданным значением - непараметрический тест Вилкоксона

В случае отклонения распределения изучаемой совокупности от нормального для проверки гипотезы о равенстве средней (медианы) определенному значению используется непараметрический критерий Вилкоксона и функция `wilcox.test`, устроенная аналогично функции `t.test`.В предположении, что распределение отклоняется от нормального, воспользуемся `wilcox.test` для нашей выборки X
```{r}
wilcox.test(x, mu = 104)
```
$p−value=0.001785<0.05=α$, следовательно гипотеза $H_0:μ=μ_0=104$ отвергается на уровне значимости $α=0.05$ (средняя не соответствует нормативу).

#### 2.2.3. Сравнение средних двух независимых нормально распределенных совокупностей -параметрический тест (t-тест Стьюдента)

После проведения теста на равенство дисперсий, проведем тест на равенство средних в двух (независимых) выборках, извлеченных из нормальных совокупностей. В нем используется t-статистика Стьюдента. 
Воспользуемся функцией `t.test()`. На входе функция получает две выборки — $𝑥$ и $𝑦$ — и два параметра: `var.equal` и `paired`. 
```{r}
t.test(x, y, var.equal = TRUE, paired = FALSE)
```
Как можно видеть, $p-value=0.008315$ меньше, чем α=0,05. Значит, гипотеза $H_0: 𝝁_𝒙 = 𝝁_𝒚$ отвергается на уровне значимости 0,05 (средние двух 
совокупностей с большой долей вероятности равны). На это же указывает и 95%
ный доверительный интервал их разности ($𝝁_𝒙 − 𝝁_𝒚$), который содержит 0. 
Наблюдаемое значение статистики критерия: `t = -0.947`.

Теперь выведем $t_{критическое}$:
```{r}
qt(0.975, 98)
```
Итак, $|t_{набл.}| = 2.6936 > t_{крит.}= 1.984467$, значит, гипотеза $𝑯_𝟎: 𝝁_𝒙 = 𝝁_𝒚$ отвергается на уровне значимости 0,05 (средние двух совокупностей с большой долей вероятности равны).

Проверим альтернативную гипотезу, что $H_1:μ_x>μ_y$:
```{r}
t.test(x, y, var.equal = TRUE, alternative = "greater", paired = FALSE)
qt(0.975, 98)
```
Как можно видеть, $p−value=0.9958>α=0.05$. Гипотеза $H_0:μ_x=μ_y$ не отвергается на уровне значимости $α=0.05$ против гипотезы $H_1:μ_x>μ_y$ (среднее первой совокупности не больше средней второй совокупности).

Для построения этого графика организуем данные в виде таблицы:
```{r}
my_data <- data.frame(group = rep(c("x", "y"), each = 50),something = c(x, y))
print(my_data)
```
Теперь построим по нашим данным график "ящик с усами", для этого подключим библиотеку `ggpubr`:
```{r}
library("ggpubr") 
```
```{r, fig.cap= "*Рис. 4. Ящичковые диаграммы (boxplots) для переменных Х и У, построенные с помощью функции `ggboxplot` пакета `ggpubr`*"}
ggboxplot(my_data, x = "group", y = "something", color = "group", palette = c("#55AFBB", "#E7B999"), ylab = "Время (час.), затрачиваемое на аудиторскую проверку 100 предприятий отрасли", xlab = "переменные X и Y")
```
По графику может увидеть небольшую разницу между средними значениями двух выборок.

#### 2.2.4. Сравнение различий двух совокупностей - непараметрический тест Вилкоксона

Для тех же выборок X и Y посмотрим медианы, интерквартильные размахи и тест Вилкоксона для несвязных и связных выборок:
```{r}
median_x = median(x)
median_x
```
```{r}
median_y = median(y)
median_y
```
```{r}
IQR_x = IQR(x, na.rm = TRUE) 
IQR_y = IQR(y, na.rm = TRUE) 
```
```{r}
IQR_x 
```
```{r}
IQR_y
```
```{r}
wilcox.test(x, y)
```
```{r}
wilcox.test(x, y, paired=TRUE)
```
Можем увидеть, что в обоих случаях (связные и несвязные выборки) значения $p-value$ меньше чем уровень значимости, следовательно гипотеза $H_0$ о незначимости различий между совокупностями отвергается.

### 2.3. Критерии проверки параметров биномиальных распределений 

На данном этапе нам предлагается проверить гипотезы из ДКР2. Сначала приведем условие задачи: Производитель  утверждает, что средний процент продаж его продукции равен 80%. Магазин провёл продажу пробной партии из 400 единиц, из которой за контрольный срок было продано 280 единиц. В предположении, что генеральная совокупность подчиняется биномиальному закону распределения, проверить на уровне значимости 0,05 следующие гипотезы: 

а)Можно ли считать, что утверждение производителя справедливо? Или процент завышен?(проверить против двух соответствующих конкурирующих гипотез).    

б) Сравнить продаваемость этой продукции с другой, которой в этом же магазине было продано за контрольный срок 258 единиц из 500 поступивших.

#### 2.3.1. Тест на равенство вероятности успеха в испытании Бернулли определенной константе
Рассматрим пункт а). Можно ли считать, что утверждение производителя справедливо? Или процент завышен?(проверить против двух соответствующих конкурирующих гипотез).

При достаточно больших объемах выборки осуществляется с помощью стандартной встроенной функции с воможностью поправки на непрерывность Йетса (Yates) (идет по умолчанию): $n=400; m=280;α=0.05$;
$H_0:p=p_0=0.8$.
Первая конкурирующая гипотеза (справедливость утверждения производителя):
$H_1:p_1≠0.8$.
```{r}
prop.test(280, 400, p = 0.8)
```
```{r}
prop.test(280, 400, p = 0.8, correct = FALSE)
```
В обоих случаях (с поправкой Йетса и без нее) $p-value$ существенно меньше, чем $\alpha$, следовательно гипотеза $H_0$ о том, что генеральная доля равна 0,8 отвергается на уровне значимости 0,05 (утверждение производителя несправедливо)

Аналогично проведем точный биномиальный тест:
```{r}
binom.test(280, 400, p = 0.8)
```
Видим, что $p-value = 2.366e-06$, что меньше уровня значимости, следовательно гипотеза отвергается на уровне значимости 0,05.

Все три теста дали близкие результаты $p-value$, которые все меньше уровня значимости $α=0.05$, – следовательно, гипотеза $H_0:p=p_0=0.8$ отвергается на уровне значимости $α=0.05$ против первой конкурирующей гипотезы. (95% интервальная оценка вероятности р также не включает в себя заявленную долю 0.8).

Для этого пункта осталось провести проверку второй конкурирующей гипотезы $H_1: p < p_0 = 0,8$
```{r}
prop.test(280, 400, p = 0.8, alternative = 'less')
```
```{r}
prop.test(280, 400, p = 0.8, alternative = 'less', correct = FALSE)
```
Для обычного случая и случая с поправкой мы наблюдаем $p-value$ = 3.957e-07 и 2.867e-07 соответственно, оба этих значения отвергают основную гипотезу о том, что генеральная доля равна 0,8, следовательно мы можем полагать, что процент продаж меньше 80.
```{r}
binom.test(280, 400, p = 0.8, alternative = 'less')
```
$p-value = 1.293e-06$, что меньше 0,05, следовательно основная гипотеза о том, что генеральная доля равна 0,8, отвергается и мы можем полагать, что процент продаж меньше 80.

#### 2.3.2. Тест на равенство вероятностей успеха в испытаниях Бернулли для нескольких биномиально распределенных совокупностей 

Рассмотрим пункт б). Сравнить продаваемость этой продукции с другой, которой в этом же магазине было продано за контрольный срок 258 единиц из 500 поступивших.

Проверка гипотезы о равенстве вероятностей (генеральных долей) биномиально распределенных генеральных совокупностей $B(n_i;_pi). H_0:p_1=p_2=...=p_k$ осуществляется с помощью `prop.test()`, где $m$ и $n$ задаются в виде векторов успехов и числа испытаний.
```{r}
prop.test(x = c(280, 258), n = c(400, 500),correct = FALSE)
```
```{r}
qchisq(0.95,1)
```
$p−value=2.221e−08<α=0.05, \chi^{2}_{набл}=31.291>\chi^{2}_{крит}=3.841459$, следовательно, нулевая гипотеза об однородности вероятностей отвергается на уровне значимости $α=0.05$, значит с большой долей вероятности средний процент продаж первой продукции не равен среднему проценту продаж второй продукции.

### 2.4. Критерии согласия эмпирического распределения с теоретическим  

#### 2.4.1. Визуализация данных с целью подбора наилучшего теоретического распределения 

Прежде чем проверять выборочное эмпирическое распределение на согласие с каким-то теоретическим, необходимо понять, какое теоретическое распределение в принципе можно подобрать – по схожести гистограммы и функции плотности (вероятности) соответствующего распределения. Многие авторы отмечают графические методы как первое, самое простое и весьма эффективное средство проверки распределения на нормальность. И здесь основными методами являются: построение гистограмм и плотностей эмпирического распределения, ящичковых диаграмм, а также построение нормальных квантильных (`Q-Q plot`) и вероятностных (`P-P plot`) графиков. 

Построим эмпирическую плотность наших данных. По ее виду, как и по виду 
гистограммы можно сделать вывод о предполагаемом теоретическом законе 
распределения.  
```{r}
volume <- data[[1]]
library("ggpubr")
```
```{r, fig.cap= "*Рис. 5. Эмпирическая плотность распределения объема основных фондов, построенная с помощью функции `ggdensity`  пакета `ggpubr`*"}
ggdensity(volume,main = "Эмпирическая плотность времени, затрачиваемого на аудиторскую проверку",xlab = "Время, затрачиваемое на аудиторскую проверку")
```
Видим, что рис.5 напоминает нормальную плотность распределения, но с существенной правосторонней ассиметрией.

Построим график типа квантиль-квантиль (`Q-Q plot`) для проверки 
соответствия квантилей выборки квантилям нормального распределения. Если 
точки эмпирического распределения близки к прямой под углом 45 градусов на 
графике – это говорит в пользу нормального распределения генеральной 
совокупности. 
```{r, fig.cap= "*Рис.6. Квантильный график (`Q-Q plot`) - эмпирическое распределение квантилей объема основных фондов и квантилей нормального распределения, построенный с помощью функции ggdensity  пакета ggpubr*"}
ggqqplot(volume)
```
Видим, что `Q-Q plot` (рис. 6) также позволяет выдвинуть гипотезу о 
нормальном распределении изучаемой совокупности. 
Итак, рассмотрим основные критерии проверки согласия выборки с 
теоретическим распределением и их реализацию в R. 


#### 2.4.2. Критерий Колмогорова- Смирнова (Kolmogorov-Smirnov (KS) test)
Проверим согласие нашей переменной из типового расчета с нормальным распределением с параметрами $N(μ=103.695; σ=1.90249)$(напомним, что для применения критерия К.-С. распределение должно быть непрерывным и с заданными параметрами). Определим их примерно подходящими к нашей выборке).

Для этого сгенерируем случайную величину с такими параметрами, посмотрим на нее и проверим согласие с нашей исследуемой
```{r}
mean(volume)
```
```{r}
sd(volume)
```
```{r}
volume_N <- rnorm(100,mean=103.695, sd=1.90249)
```
```{r}
glimpse(volume_N)
```
```{r}
ks.test(volume, volume_N)
```
В итоге получаем, что $p−value=0.4676$, следовательно, на любом разумном уровне значимости можно принять гипотезу, что наша переменная имеет нормальное распределение с параметрами $N(μ=103.695; σ=1.90249)$.

#### 2.4.3. Критерий Шапиро-Уилка (Shapiro - Wilk test)
```{r}
shapiro.test(volume)
```
Видим, что $p−value=0.04347<α=0.05$, поэтому гипотеза о нормальном распределении отвергается с вероятностью ошибки $α=0.05$.

#### 2.4.4. Тест Жарка-Бера (Jarque-Bera test)
Тест Жарка-Бера предусматривет проверку нормальности распределения двумя методами: 
классический и робастый.
```{r}
library(DescTools)
```
```{r}
JarqueBeraTest(volume, robust = FALSE)
```
```{r}
JarqueBeraTest(volume, robust = TRUE)
```
В обоих случаях $p−value>α=0.05$ (равняется 0.08154 и 0.1002 для классического и робастого методов соотвественно), поэтому гипотеза о нормальности распределения не отвергается с вероятностью ошибки $α=0.05$.

#### 2.4.5. Критерий Андерсона-Дарлинга
```{r}
library(nortest)
```
```{r}
ad.test(volume)
```
$p−value=0.1594>α=0.05$, поэтому гипотеза о нормальности распределения не отвергается с вероятностью ошибки α=0.05.

#### 2.4.6 Критерий Лиллиефорса
```{r}
lillie.test(volume)
```
$p−value=0.2472>α=0.05$, поэтому гипотеза о нормальности распределения не отвергается с вероятностью ошибки α=0.05.

#### 2.4.7 Критерий $\chi^2$ Пирсона
```{r}
pearson.test(volume)
```
Видим, что $p−value=0.5879>α=0.05$, поэтому гипотеза о нормальности распределения не отвергается с вероятностью ошибки $\alpha= 0.05$.

#### 2.4.8. Д’Агустино тест (D’Agostino’s K2 test)
```{r}
library(moments)
```
```{r}
agostino.test(volume)
```
$p−value=0.02438<α=0.05$, поэтому гипотеза о нормальности распределения отвергается с вероятностью ошибки α=0.05.

Подводя итоги, разные критерии дали разные $p−value$ и разные результаты о принадлежности распределения к нормальному (критерии Д’Агустино и Шапиро-Уилка отвергли гипотезу о нормальном распеределении, критерии Колмогорова- Смирнова, Жарка-Бера, Пирсона, Лиллиефорса, Андерсона-Дарлинга и о не отвергли эту гипотезу) в связи с разной мощностью, чувствительностью и другими особенностями изученных критериев, поэтомы мы не можем с уверенностью говорить о принадлежности распределения нормальному, гипотеза $H_0$ отвергается.